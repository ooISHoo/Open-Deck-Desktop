# CLAUDE.md — Open-Deck-Desktop フォーク

## このリポジトリの位置づけ

`kawa-nobu/Open-Deck-Desktop` (MIT / Electron) のフォーク。
本家に独自機能を足す。**本家に追従し続ける前提**なので、本家の変更とコンフリクトしない書き方を最優先する。

- 本家: https://github.com/kawa-nobu/Open-Deck-Desktop
- 追従元ブランチ: `Release`
- 環境: Windows ネイティブ + Node.js。WSL では作業しない（後述）

---

## 不変ルール

1. **差分を最小にする。** 整形・リネーム・リファクタ・コメント削除を"ついでに"やらない。本家との rebase コストが全て。
2. **既存のコードスタイルに従う。** 日本語コメント、`snake_case`、既存の書式（セミコロン有無・インデント）をそのまま真似る。lint/prettier を導入しない。
3. **依存を増やさない。** `package.json` の dependencies / devDependencies を触らない。
4. **本家のファイルを新設・分割しない。** 既存ファイルへの追記で完結させる。
5. **x.com など SNS 側の DOM 構造に依存しない。** Electron の API だけで完結させる。SNS の内部 class 名や `data-testid` に依存した実装は却下。
6. **マイルストーンは1個ずつ。** 1個終わったらコミットして停止。`/clear` してから次へ。まとめて実装しない。
7. **PLAN モードは使わない。**

## 停止して報告すべきケース

以下に該当したら、勝手に判断せず**作業を止めて相談すること**。

- 本家が `main.js` を分割した／設定ストアの仕組みを作り変えた（→ 移植方針の再設計が要る）
- 「作業概略」に書いた前提と実際のコードが食い違う
- 既存の挙動を変えないと実装できない

---

## 前提知識（本家の設計）

本家の実装が変わっていても、**この意味論を持つ場所を探して**作業すること。行番号・関数名は変わりうるので当てにしない。

- **カラムの中身は `<webview>`**。タイムラインは `<webview src="https://x.com/home">` で、`preload_webview.js` が注入されている。カラム生成の HTML テンプレートは `opd_resource/content.js` にある。
- **外部URLを開く経路は1本に集約されている。** 各 preload が `open_default_browser` という IPC を送り、`main.js` の `ipcMain.on` ハンドラが `open_external_with_warning()` を呼ぶ。この関数がスキーム制限・警告ダイアログ・警告オフ設定を見て `shell.openExternal` する。**ここが唯一の絞り込み点**。
- **システム設定の追加は4点セット。** 「既定値オブジェクト」「保存用 switch のケース」「設定画面 HTML のUI」「設定画面 JS の読込 case + change ハンドラ」。
- **設定のマイグレーションは自動。** 既定値オブジェクトのキーを走査して、既存ユーザーの JSON に無いキーを補完・書き戻す処理がある。**既定値に1行足すだけで移行は完了する。** バージョン判定コードを書かないこと。
- **設定の読み方の前例。** 「閉じるボタン最小化」がイベント発火時に設定をディスクから都度読んで bool 分岐している。この形を踏襲すれば再起動不要で即反映される。

---

## タスク

### M0: 環境確認

- `npm ci` → `npm run dev` で起動すること
- x.com にログインしてタイムラインカラムが出ること（以降の目視確認に必要）

**完了条件:** 未改造の状態でアプリが正常に動く。

---

### M1: カラム内の選択テキスト用コンテキストメニュー — 本家に吸収済み（一部のみ残置）

本家がコンテキストメニュー（コピー / 編集操作 / リンク / 画像）を `app.on("web-contents-created")` で実装したため、
独自の `web-contents-created` ハンドラは削除した。二重にメニューが出るのを避けるため、**独自ハンドラを足さないこと**。

**残している独自差分は「Googleで検索」1項目だけ。** 本家のテンプレート組み立て内、選択テキスト分岐に相乗りしている。

- クエリは trim → 100文字で切り詰め → URLエンコード
- 遷移は `open_external_with_warning()` を通す。本家の警告ダイアログ / 警告オフ設定 / スキーム制限がそのまま効く
- 空白のみの選択では項目を出さない

**完了条件（実機で目視）**

- タイムラインの本文を選択 → 右クリック → 本家の項目に加えて「Googleで検索」が出る
- 既定ブラウザで検索結果が開く（警告設定に従う）
- 選択なし・画像・リンク上の右クリックで本家の挙動が変わっていない
- メディアビューア機能と干渉していない

---

### M2: 外部URL確認ダイアログの表示ON/OFF設定 — 本家に吸収済み（独自実装は削除）

本家が `bypass_url_open_msg`（「URLを開く際にセキュリティ警告を出さない(非推奨)」/ 既定 OFF）を実装した。
独自の `open_url_confirm_dialog`（真偽が逆向き / 既定 ON）は等価機能なので削除済み。**再実装しないこと。**

本家はさらに `http:` / `https:` 以外のスキームを警告オフ設定に関わらず必ず確認する制限を入れている。
独自実装にはこれが無かったため、本家版の方が安全側。

---

### M3: Google検索を外部URLオープン経路に統一 — 完了

M1 の残置項目が `open_external_with_warning()` を直接呼ぶ形で達成済み。

---

## 作業しないこと

- WSL / Linux 上での動作確認。今回の機能は **OSネイティブのメニューと既定ブラウザ起動**なので、Linux で確認しても意味がない。Windows 実機で確認する。
- Linux / macOS ビルド。CI（`.github/workflows/release.yml`）が matrix で焼く。
- コード署名まわり。本家が未署名運用（`CSC_IDENTITY_AUTO_DISCOVERY=false`）なのでそのまま。
